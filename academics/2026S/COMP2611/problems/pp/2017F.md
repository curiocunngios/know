# Question 3
```
palindrome # (initialization)

# initialize i 
sub $t0, $t0, $t0
# initialize j 
addi $t1, $a1, -1
# initialize $v0
addi $v0, $zero, 1

# while_loop:


# checks to see if i<j, and returns whenever needed
slt $t3, $t0, $t1
beq $t3, $zero, return

# load word[i],word[j]
add $t4, $t0, $a0
add $t5, $t1, $a0
lw $t4, 0($t4)
lw $t5, 0($t5)




# update i,j 
# compare word[i],word[j] loaded 
# then either go back to the while loop  
# or return with the proper value in $v0
addi $t0, $t0, 1
addi $t1, $t1, -1
beq $t4, $t5, while_loop
addi $v0, $zero, 0

return:
    jr $ra
```