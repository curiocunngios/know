# 2017S

## Question 2
```
base:
    addi $v0, $a0, 0
    beq $a1, $zero, return
non_base_case:
# back up appropriate registers to the stack

addi $sp, $sp, -4
sw $ra, 0($sp)


# do the division operation, set up new x, y for the next recursive call
div $a0, $a1
mfhi $t0
addi $a0, $a1, 0
addi $a1, $t0, 0
jal euclidean



# done with recursion, restore appropriate register(s) from the stack
lw $ra 0($sp)
addi $sp, $sp, 4



return:
    jr $ra
```